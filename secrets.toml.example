# =============================================================================
# Streamlit secrets — copy to .streamlit/secrets.toml and fill in values.
# On Streamlit Community Cloud, paste these into Settings → Secrets.
# DO NOT commit secrets.toml to git.
# =============================================================================

[contact]
email = "jim@jimappiah.com"
calendly_url = "https://calendly.com/jim-appiah/discovery-call"

[webhook]
# Optional — paste a Zapier/Make.com webhook URL here to capture leads.
# Leave empty to disable. Each completed assessment will POST a JSON payload.
lead_capture_url = ""
